#!/usr/bin/env bash
set -o errexit -o nounset -o pipefail

RENDLER_HOME=${RENDLER_HOME:="$( cd "$( dirname "$0" )/.." && pwd )"}

dot -Tpdf "$RENDLER_HOME/result.dot" > "$RENDLER_HOME/result.pdf" && echo "Wrote graph to '$RENDLER_HOME/result.pdf'"
